export default class BasicPokemon {
  /**
   * @param {String} imageURL
   * @param {String} name
   * @param {String} id
   */
  constructor(imageURL, name, id) {
    this.imageURL = imageURL;
    this.name = name;
    this.id = id;
  }
}
